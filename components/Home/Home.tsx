import React from 'react';
import {View, Text} from 'react-native';
import data from '../../data';

export default class App extends React.Component {
	public render(): React.ReactNode {
		return (
			<View style={{flex:1}}>
				<Text >SoccerField</Text>
			</View>
		);
	}
}
